import React from 'react';
export default () => (
    <i className="push">
        Click any of the buttons below. Using random durations and outcomes, the
        dummy callback will sometimes succeed and sometimes fail.
    </i>
);
